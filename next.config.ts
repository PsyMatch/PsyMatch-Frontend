import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: [ 'lh3.googleusercontent.com' ,'ui-avatars.com', 'example.com', 'res.cloudinary.com'],
  },
};

export default nextConfig;
